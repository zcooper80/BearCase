{
  "name": "bearcase",
  "version": "0.2.0",
  "private": true,
  "description": "AI-era short portfolio analyzer with live market data.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": { "node": ">=18" },
  "dependencies": {
    "express": "^4.19.2"
  }
}
